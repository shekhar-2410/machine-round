import { useState } from "react";

const CountdounTimer = () => {
  // Render the timer and buttons in the component
  return <div className="App">countdowntimer</div>;
};

export default CountdounTimer;
